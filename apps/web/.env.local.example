# Production and Docker Compose: keep this empty so the browser uses the
# same-origin gateway paths (/api/... and /health).
NEXT_PUBLIC_API_URL=

# Local development only, when the frontend runs outside Docker:
# NEXT_PUBLIC_API_URL=http://localhost:8000
