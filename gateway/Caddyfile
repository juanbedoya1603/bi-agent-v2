:80 {
    encode gzip

    @api path /api/*
    handle @api {
        reverse_proxy backend:8000
    }

    @health path /health
    handle @health {
        reverse_proxy backend:8000
    }

    handle {
        reverse_proxy frontend:3000
    }
}

